#include<bits/stdc++.h>
using namespace std;

void solution() {
    int x;
    cin >> x;

    cout << (x + 4) / 5 << '\n';
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    solution();
    return 0;
}